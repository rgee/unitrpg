using strange.extensions.signal.impl;

namespace Contexts.Global.Signals {
    /// <summary>
    /// Adds a new scene to the current one.
    /// </summary>
    public class AddSceneSignal : Signal<string> {
    }
}