namespace Contexts.Battle.Models
{
    public enum HighlightLevel {
        
        GlobalEnemyMove = 0,
        SpecificEnemyMove,
        PlayerMove,
        PlayerAttack,
        PlayerHover,
        PlayerInteract
    }
}