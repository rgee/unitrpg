namespace Contexts.Battle.Models {
    public enum CombatUITriggers {
        PhaseTransitionComplete,
        UnitSelected,
        MoveSelected,
        AttackSelected,
        TargetSelected,
        MovementComplete,
        FightComplete
    }
}