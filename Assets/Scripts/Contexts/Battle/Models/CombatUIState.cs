namespace Contexts.Battle.Models {
    public enum CombatUIState {
        Default,
        PhaseChanging,
        ActionMenu,
        ContextMenu,
        MoveRange,
        AttackRange,
        Moving,
        CombatForecast,
        Fighting,
        NpcLocked
    }
}