using strange.extensions.signal.impl;
using UnityEngine;

namespace Contexts.Battle.Signals {
    // Signals that a unit was selected to use
    public class UnitSelectedSignal : Signal<Vector3> {
    }
}
