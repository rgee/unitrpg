using strange.extensions.signal.impl;

namespace Contexts.Battle.Signals {
    /// <summary>
    /// Used for returning to a previous UI state or canceling an action.
    /// </summary>
    public class BackSignal : Signal {
    }
}
